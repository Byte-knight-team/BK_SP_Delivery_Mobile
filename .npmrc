# Suppress strict peer dependency resolution errors.
# Required because expo-router's transitive dependencies (vaul, @radix-ui)
# pull in react-dom@19.2.7 which conflicts with the project's react@19.2.3.
# This flag tells npm to use the legacy (npm v6) resolver and skip peer checks.
legacy-peer-deps=true
